export type LinkedListShiftConfig = {
  /** How many items to return. */
  batchSize?: number,
  /** If the batchSize is the minimum amount that must be met to return anything. */
  batchSizeIsMinimum?: boolean,
};
